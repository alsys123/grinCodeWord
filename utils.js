
/* ------------------------------
   Utils

   NOTE: Make sure this is first file loaded
   ------------------------------ */


function dei(element) {
    return document.getElementById(element);
}

function cLog(...text) {
    console.log(...text);
}
